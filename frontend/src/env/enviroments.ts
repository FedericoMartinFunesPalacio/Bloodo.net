export const enviroment = {
  // URL del API Gateway (puerto 8080)
  apiGatewayUrl: '/api',
  // URL a traves del proxy del dev-server (evita problemas CORS)
  appServiceUrl: '/api/v1',
  apiUrlDummy: '/api/v1',

};
